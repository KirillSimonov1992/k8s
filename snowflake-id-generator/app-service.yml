apiVersion: v1
kind: Service
metadata:
  name: snow-demo-service
spec:
  selector:
    app: snow-demo
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
